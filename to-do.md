# PRIORITY
- ensure that warning for non-matching CRSs can be ignored 
- run: (1) americas, (2) afrotropics, (3) islands
- start building shiny app?

# tests
- model e^2 using a lognormal distribution

# products
- long-term average raster for GIS people: exclude `s(year)`, `s(doy)`, and `ti(year,doy)`
- shiny app for predicting from the model given the data
- gif of mean and var over the years and over doy
- integrate shiny app into MoveBank? (use MoveApps?)
- rasters with contour layer instead of boundaries of ecoregions:
  - can we detect the existing biomes?
  - can we identify new biomes based on DENVar?
  - hex plots by group?
