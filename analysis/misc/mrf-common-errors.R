#' `Error in smooth.construct.mrf.smooth.spec(object, dk$data, dk$knots) :`
#' `  mismatch between nb/polys supplied area names and data area names`
#' occurs if there is an element of `xt$nb` that is not in `levels(fact)`
